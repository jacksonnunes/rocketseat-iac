# Don't run protobufjs's unnecessary install script (pnpm 11 fails install unless it's listed here).
# https://github.com/protobufjs/protobuf.js/pull/2299
allowBuilds:
  '@pulumi/docker': false
  '@pulumi/docker-build': false
  protobufjs: false
